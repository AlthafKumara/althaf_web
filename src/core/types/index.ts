/**
 * @fileoverview Global Shared Types
 *
 * This file exports generic, cross-cutting TypeScript types that are NOT
 * tied to any specific feature domain. Feature-specific types belong in
 * their respective `features/<name>/models/` directories.
 *
 * Currently empty — reserved for future shared types such as:
 * - `ApiResponse<T>` wrapper
 * - `Pagination` metadata
 * - Generic error types
 *
 * @module core/types
 */

export {};
